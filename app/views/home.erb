<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Home</title>
  </head>
  <body>
    <% if @token != nil %>
    <p>Login successful</p>
    <form action="/recipients/new" method="get">
      <input type="submit" value="Add recipient">
    </form>
    <% else %>
    <p>Login failed. Check your api key and try again.</p>
    <% end %>

    <% if @recipient != nil %>
    <p><%= @recipient %> has been added successfully</p>
    <% end %>
  </body>
</html>
